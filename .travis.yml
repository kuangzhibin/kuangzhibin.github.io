language: node_js
node_js: stable

before_install:
  - export TZ='Asia/Shanghai'  # 设置时区
  - npm install hexo-cli -g

# S: Build Lifecycle
install:
  - npm install

#before_script:

script:
  - hexo g

after_script:
  - hexo d

# E: Build LifeCycle

branches:
  only:
    - dev
